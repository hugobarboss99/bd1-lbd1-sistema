# O front usa dados falsos (mock) por padrao, ate o backend existir.
# Quando o backend estiver pronto: copie para .env, ajuste a URL e troque para false.
VITE_API_URL=http://localhost:8000
VITE_USE_MOCK=false
